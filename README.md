# POC: Generic Handler

It demonstrate how to implement a kind-of type-safe processor to handle unknown user input.

The goals is to define an extensible function executor based on user generic input.

## How to run

| Description | Command |
| :--- | :--- |
| Install dependencies | `npm install` |
| Run tests | `npm test` |
